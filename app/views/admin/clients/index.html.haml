= render "shared/admin_menu"
#page-wrapper
  .row
    .col-lg-12
      %h2 Clients
      .panel.panel-default
        .panel-heading
          %i.fa.fa-bar-chart-o.fa-fw
          %span
            = link_to 'Add New', new_admin_client_path, :class => "btn btn-primary btn-sm"
        / /.panel-heading
        .panel-body
          .row
            .col-lg-12
              .table-responsive
                %table.table.table-bordered.table-hover.table-striped
                  %thead
                    %tr
                      %th #
                      %th Name
                      %th Username
                      %th Actions
                  %tbody
                    -unless @users.blank?
                      - @users.each_with_index do |user, index|
                        %tr
                          %td
                            = index + 1
                          %td
                            = user.name
                          %td
                            = user.email
                          %td
                            = link_to("edit", edit_admin_client_path(user))
                            = link_to 'Delete',url_for(action: :destroy,id: user.id),method: :delete, data: {confirm: "Are you sure?"}